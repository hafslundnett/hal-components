/* tslint:disable:no-unused-variable */

import { DisplayConfigFindCssPipe } from './display-config-find-css.pipe';

describe('Pipe: DisplayConfigFindCss', () => {
  it('create an instance', () => {
    const pipe = new DisplayConfigFindCssPipe();
    expect(pipe).toBeTruthy();
  });
});
